import React from 'react';
import { render } from 'react-dom';

const App = () => {
    return(
        <React.Fragment>
            <h1 className="title">Hello World! (With React)</h1>
            <p className="text">These are my first lines of code with React while using Webpack and Babel!</p>
        </React.Fragment>
    )
    
}

export default App;